node_modules/
dist/
# Runtime data dir (SQLite files, generated artifacts) — root only.
/data/
.env
.env.local
.worker-auth-secret.txt
.DS_Store
*.log
.replit-cache/
.cache/
sessions/
logs/
# drizzle/ migration files ARE checked in. They're how prod schema changes
# are applied (drizzle-kit migrate, not drizzle-kit push).
.secrets/

# Playwright (Q9)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
_rec/

# Scratchpad — throwaway agent artifacts, never commit
scratchpad/

# TIGER source archives are fetched on demand by scripts/tiger-net/build.mjs.
# Only the built artefact (assets/tiger/usnet.bin.gz) is committed.
assets/tiger/.cache/
